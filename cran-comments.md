## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new submission.

This package requires either a pre-installed `automerge-c` library or build tools
  (Rust >= 1.80, CMake >= 3.25) to compile from source.
